spring:
  profiles:
    default: local

  servlet:
    multipart:
      max-request-size: 100MB
      max-file-size: 100MB

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  expiration_time: 864000000 #1일
  secret: ef2dd657579a94647d4e3e2db594b81993a6ea929549562b55fb78ca6cad6198

---

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/pjt?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy

  jpa:
    hibernate:
      ddl-auto: update

---

spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/pjt_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy

  jpa:
    hibernate:
      ddl-auto: update